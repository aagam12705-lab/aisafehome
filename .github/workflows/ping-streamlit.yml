name: Ping Streamlit App

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit app
        run: |
          curl -L --max-time 30 --retry 2 --retry-delay 10 "https://ai-safehome.streamlit.app/"
